# BD68 Dev v1.0

Custom Codex skill for the BD68 and WildSoul workflow.

## Included
- `SKILL.md`
- `agents/openai.yaml`

## Purpose
- keep retrieval order consistent with `memoryai`, `chub`, and `vfs`
- document the current Codex setup
- standardize the short GPT-5.4 token cost summary

## Install
Copy this folder into `~/.codex/skills/bd68-dev-v1-0` or install it from GitHub with the Codex skill installer.