# BD68 Dev v1.0 Loader For OpenCode

Load `@bd_dev_kit/BD68_PROFILE.md` at the start of the session.
Apply the `Codex Thread Payload Hygiene` rules from that profile during request assembly.

Before using a bundled reference, open `@bd_dev_kit/references/SOURCE_INDEX.md`.
Treat the files in `@bd_dev_kit/references/` as curated GitHub-backed local retrieval sources, not as optional notes.

Use these references only when needed:
- `@bd_dev_kit/references/impeccable.md` before frontend brainstorming or design planning.
- `@bd_dev_kit/references/concise-planning.md` before writing a plan, roadmap, or task breakdown.
- `@bd_dev_kit/references/antigravity.md` only when the current tool or skill stack is insufficient and a repeated need exists.

Treat `Shubhamsaboo/awesome-llm-apps` as an idea catalog only, not as a technical baseline or source of truth.
Before implementation, confirm API and SDK behavior with official docs.
For third-party API docs, use `chub` as default retrieval source; use Context7 only as fallback/accelerator when `chub` has no actionable result.
For local code retrieval/edit, prefer `serena` once onboarded.
Use gstack only as lightweight orchestration gates (`product-gate`, `engineering-gate`, `ship-gate`) and never all-ways-on.
Do not let gstack overlap with `lint-and-validate`, `webapp-testing`, or `github`.
If retrieval does not provide verifiable evidence for a technical claim, answer `không đủ dữ liệu` instead of guessing.
For anti-guessing in Python agent flows, prefer `pydantic-ai`, `instructor`, and `langsmith` (`langgraph` optional for orchestration-heavy workflows).
Apply the 5-skill add-on gate from `@bd_dev_kit/BD68_PROFILE.md`: always-on `context-optimization` + `context-window-management`; conditional `context-compression`, `prompt-caching`, and `hierarchical-agent-memory` with hysteresis and memory-stability checks.
Treat the BD68 profile as the default operating model for BD68 and WildSoul work in this environment.

## Project Overlay
At session start, check if `.bd68/PROJECT.md` exists in the current working directory.
If it exists:
  - Load it AFTER BD68_PROFILE
  - Values in PROJECT.md override global profile where specified
  - `docs:` entries define preferred retrieval sources for this project
  - `locked:` entries are architecture decisions — never suggest changing them
  - `never:` entries are hard constraints — never violate them
  - `active_context:` gives current milestone and focus — use to prioritize responses
If it does not exist, proceed with global BD68_PROFILE only.

## Skill & MCP Installation Policy
When user requests adding a new skill, MCP, or any installable component:

STEP 1 — Confirm target before acting:
  Ask: "Cài global (mọi project dùng) hay chỉ project này?"
  Default nếu user không nói rõ: global → ~/.codex/skills/<skill-name>/

STEP 2 — Install theo target:
  Global skill:        ~/.codex/skills/<skill-name>/SKILL.md
  Global MCP:          ~/.codex/config or mcp config (per tool convention)
  Project-only note:   .bd68/PROJECT.md → thêm vào section key_libs hoặc docs

STEP 3 — Sau khi install global, tự động sync về source kit:
  $skillName = "<tên skill vừa install>"
  $src = "$env:USERPROFILE\.codex\skills\$skillName"
  $dst = "C:\Users\ngath\Desktop\Bach Duong IT\2026\Project\bd_dev_kit\skills\$skillName"

  if (Test-Path $src) {
      Copy-Item -Path $src -Destination $dst -Recurse -Force
      Write-Host "[BD68] Synced to source kit: $skillName" -ForegroundColor Green
  } else {
      Write-Host "[BD68] WARNING: $src not found — sync skipped" -ForegroundColor Yellow
  }

  MCP global:
  - chạy `install-agent-pack.ps1 -Target codex` từ source kit
  - installer sẽ export toàn bộ `[mcp_servers.*]` hiện có từ `~/.codex/config.toml`
    vào `bd_dev_kit/templates/codex.mcp_servers.toml`
  - lần cài trên máy mới sẽ import snapshot đó trở lại runtime config

  Sau khi sync xong, remind user:
  "[BD68] Skill/MCP global đã sync về source kit.
   Nhớ commit bd_dev_kit lên git để máy mới có đủ thành phần."

NEVER:
  - Cài trực tiếp vào project folder mà không hỏi
  - Giả định target mà không confirm
  - Bỏ qua bước remind sync sau install global

## Memory System
At session start, load memory files if they exist:
  - `~/.codex/memories/MEMORY.md` — your accumulated knowledge (~800 token budget)
  - `~/.codex/memories/USER.md` — user profile and preferences (~500 token budget)

Reading memory: treat both files as frozen context injected at session start.

Managing memory (during session):
  MEMORY.md entries — use when you learn something new about the environment, project, or workflow:
    - Add: append `[YYYY-MM-DD] category: note` (keep entries concise)
    - Replace: overwrite stale entries with updated info
    - Remove: delete entries that are no longer relevant
    - Consolidate: when approaching 2200 chars, merge related entries

  USER.md entries — update only when user explicitly states a preference or constraint change.
    - Never infer preferences — only record what user explicitly states
    - Budget: 1375 chars max

Format for new entries: `[YYYY-MM-DD] category: one-line note`
Common categories: project | workflow | decision | constraint | preference | bug | fix

Auto-write trigger — Codex PHẢI ghi MEMORY.md khi session kết thúc nếu có ÍT NHẤT 1 trong các điều kiện sau:
  TRIGGER A — Quyết định kỹ thuật: đã chọn library/pattern/architecture trong session này
  TRIGGER B — Bug fix quan trọng: fix lỗi mất >3 tool calls để tìm ra
  TRIGGER C — Workflow mới: user đã chỉnh cách làm việc hoặc đặt constraint mới
  TRIGGER D — Session dài: >15 tool calls với kết quả có thể dùng lại

Không ghi nếu:
  - Session chỉ là câu hỏi đơn giản / giải thích / không có output thực
  - Không có quyết định nào được chốt

Format bắt buộc khi ghi:
  [YYYY-MM-DD] category: one-line note (max 120 chars)
  category = project | decision | fix | pattern | constraint | workflow

Ví dụ entries đúng:
  [2026-03-15] decision: WildSoul V2 dùng PixiJS cho canvas animation sau khi eval Konva và Phaser
  [2026-03-15] fix: React useEffect cleanup required khi unmount PixiJS app — không thì memory leak
  [2026-03-15] constraint: Không thêm npm dependency mới khi chưa confirm với user

Quy trình ghi cuối session:
  1. Scan lại conversation — có trigger nào không?
  2. Nếu có → draft entries (tối đa 3 entries per session)
  3. Kiểm tra budget: nếu MEMORY.md > 1800 chars → consolidate entries cũ trước khi thêm
  4. Append vào MEMORY.md
  5. Thông báo ngắn cho user: "[BD68 Memory] Đã ghi X entries vào MEMORY.md"

NEVER:
  - Invent memory entries not grounded in actual session content
  - Let memory files exceed their char budgets without consolidating first
  - Store sensitive data (API keys, passwords, personal info beyond stated preferences)
  - Ghi >3 entries mỗi session — ưu tiên chất lượng hơn số lượng


